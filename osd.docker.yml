server.host: "0.0.0.0"
server.port: 5601

# OpenSearch with security enabled — use HTTPS
opensearch.hosts: ["https://opensearch:9200"]
opensearch.ssl.verificationMode: none
opensearch.username: "admin"
opensearch.password: "0p3nPipes!Viz#7"

logging.verbose: false
